# Pinned minimum versions so builds are reproducible.
# Keep this list minimal — add a dependency only with a clear reason.
# 1.39+ is required for st-key CSS classes (used to scope the custom nav styling)
# and column vertical_alignment used by the header layout.
streamlit>=1.39.0
fastf1>=3.4.0
plotly>=5.22.0
pandas>=2.2.0
python-dotenv>=1.0.1
